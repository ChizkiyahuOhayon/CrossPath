\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_crosspath_framework.pdf}
  \caption{\textbf{CrossPath exposes off-diagonal compatibility between frozen
  CIR endpoints.} A composed query and gallery are encoded by two endpoints
  (left), all four $q_i^\top g_j$ paths form a compatibility matrix (center),
  and zero-parameter cross-mean fusion produces one ranking (right). In the
  shown FashionGen case, the target moves from rank 9 to rank 1.}
  \label{fig:crosspath_framework}
\end{figure*}
